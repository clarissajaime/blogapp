h1 Edit Post

form(action="/post/"+_id method="post")
  div
    label(for="title") Title:
    input(type="text", name="title", value=title, required) 
  div
    label(for="content") Content:
    textarea(name="content", required)= content
  button(type="submit") Save Post
  
